@model IEnumerable<BookClub.Data.Models.Discussion>

@{
    ViewBag.Title = "GetAllDiscussions";
}

<h2>Discussion Boards</h2>


<table class="table table-hover" id="discussionTable">
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.Title, "Topic",

                    new { id = item.Id , Controller = "Discussion" })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
            </tr>
        }
    </tbody>
</table>
